package com.dpashko.fastrent.presentation.feature.onboarding

import androidx.lifecycle.ViewModel
import dagger.hilt.android.lifecycle.HiltViewModel
import javax.inject.Inject

/** ViewModel class for the onboarding screen, responsible for managing the onboarding state
 * and handling user interactions with the screen.
 */
@HiltViewModel
class OnboardingViewModel @Inject constructor() : ViewModel() {
    // Add ViewModel logic here
}
